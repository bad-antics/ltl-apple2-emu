// examples/demo.ltl — runnable demo for ltl_apple2_emu

import std::io
use ltl_apple2_emu as lib

fn main() {
    io::println("ltl_apple2_emu demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}