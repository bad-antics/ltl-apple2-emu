// ltl-apple2-emu/main.ltl
//
// Partial Apple II 6502 system emulator — RAM + TEXT mode.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-apple2-emu v0.1.0")
    io::println("Partial Apple II 6502 system emulator — RAM + TEXT mode.")
    0
}
